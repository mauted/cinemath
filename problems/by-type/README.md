# Problem batches by catalog planner

Each subfolder is a catalog `plan_*` solver. Inside, `pack_id` folders group problems from a single source topic (Lamar section, MIT topic, etc.).

Run `uv run python scripts/sync_problem_banks.py` to refresh Lamar scrapes.
